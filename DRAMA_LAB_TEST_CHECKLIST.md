# 短剧实验室真实功能测试清单

## 使用范围

- 测试入口：`/drama-lab`
- 测试环境：填写实际地址，例如 [`http://127.0.0.1:3002/drama-lab`](http://8.163.37.148:3001/drama-lab)
- 测试账号：使用已有测试账号；不要把密码、Token 或私钥写入本文件。
- 需要可用的默认文本模型、图片模型和视频模型渠道；没有配置渠道时，记录接口返回的配置错误，不判定为前端通过。
- 每个用例执行后填写“结果”和“备注”，失败时附浏览器截图、请求地址、响应状态和生成记录 ID。

## 1. 入口与项目持久化

| 编号 | 操作 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| P-01 | 登录后打开 `/drama-lab` | 页面可加载，项目列表和“新建短剧”入口可见；未登录请求被拦截 | [ ] |
| P-02 | 新建短剧，填写标题、简介、视觉风格和画面比例 | 创建请求成功并进入该项目；列表中出现项目摘要、集数、分镜数和比例 | [ ] |
| P-03 | 刷新项目列表，再打开刚创建的项目 | 项目仍存在，详情和设置与保存前一致 | [ ] |
| P-04 | 修改项目标题、简介、风格、比例 | 保存成功；刷新项目详情后修改仍保留 | [ ] |
| P-05 | 新增一集、删除一集；用“批量导入剧集”导入多行内容 | 集数、顺序、标题和剧本文字正确持久化；删除前有确认 | [ ] |
| P-06 | 在两个页面同时修改同一项目（可选） | 后提交的一方收到冲突提示，不覆盖已经保存的版本 | [ ] |

## 2. 剧本生成、编辑与导入

| 编号 | 操作 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| S-01 | 在当前集填写故事梗概，点击“生成剧本” | 创建一次文本生成请求；返回完整剧本文字并自动保存到当前集 | [ ] |
| S-02 | 不填写故事梗概直接生成 | 前端阻止提交并提示先填写梗概；不产生上游请求和生成记录 | [ ] |
| S-03 | 编辑生成后的剧本文字，等待自动保存或手动保存 | 保存成功；刷新后编辑内容仍在 | [ ] |
| S-04 | 打开“从其他项目导入剧本”，选择有剧本的项目并确认 | 只导入故事梗概和各集剧本文字，不覆盖当前项目的角色、场景、道具、分镜和媒体 | [ ] |
| S-05 | 选择没有梗概和剧本的项目导入 | 导入被拒绝并给出明确提示，当前项目不被修改 | [ ] |
| S-06 | 在后台生成/调用记录中筛选短剧文本任务 | 能看到剧本生成的成功或失败记录、模型、耗时和项目关联；失败也有记录 | [ ] |

## 3. 角色、场景、道具资产

| 编号 | 操作 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| A-01 | 在有剧本的集上分别执行“从剧本提取”角色、场景、道具 | 分别创建对应资产；同名资产不会重复追加；项目刷新后仍存在 | [ ] |
| A-02 | 在没有剧本的集上提取资产 | 操作被阻止，不产生上游请求 | [ ] |
| A-03 | 手动新增、编辑、删除角色、场景、道具 | 表单校验生效；保存后列表和刷新结果一致 | [ ] |
| A-04 | 从素材库向对应资产类型导入图片/文本素材 | 导入的名称、描述和图片引用正确；不允许同名重复导入 | [ ] |
| A-05 | 对资产执行“AI 生图” | 创建真实图片生成任务；候选图写入该资产；任务和结果可在生成记录中找到 | [ ] |
| A-06 | 上传资产参考图，切换主参考图，删除候选图 | 图片可访问；主参考图状态正确；刷新后引用关系保持 | [ ] |
| A-07 | 同时对三个不同资产点击“AI 生图” | 三个任务分别完成时，三张结果都保留并显示；刷新后不能只剩最后一张；记录中有三个不同请求/任务 ID | [ ] |
| A-08 | 资产已被分镜引用时打开影响列表，点击受影响镜头和“重新生成分镜图” | 能定位到对应镜头；重新生成只针对该镜头创建任务，不修改其他镜头 | [ ] |

## 4. 分镜拆解与编辑

| 编号 | 操作 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| B-01 | 在已准备剧本和资产的集上点击“从剧本提取分镜” | 创建文本拆镜请求；返回镜头顺序、时长、场景、角色/道具引用等结构化数据并保存 | [ ] |
| B-02 | 在没有剧本的集上提取分镜 | 操作被阻止，不产生上游请求 | [ ] |
| B-03 | 新增、编辑、删除分镜；修改镜头描述、镜头类型、机位、动作、对白、时长和图片/视频提示词 | 表单保存成功；刷新后字段和顺序一致 | [ ] |
| B-04 | 检查分镜引用不存在的资产 ID 的异常响应 | 接口拒绝无效引用并给出错误；已有合法分镜不被破坏 | [ ] |
| B-05 | 在生成记录中查看分镜提取任务 | 成功和失败均有短剧文本调用记录，且与项目/剧集关联 | [ ] |

## 5. 分镜图、帧和分镜视频

| 编号 | 操作 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| G-01 | 为镜头准备主参考图后点击“生成分镜图” | 创建图片任务；状态经历排队/生成/完成或失败；完成后图片 URL 写入当前镜头 | [ ] |
| G-02 | 没有镜头所需参考图时生成分镜图 | 操作被阻止并指出缺少的资产，不产生图片任务 | [ ] |
| G-03 | 为镜头分别生成首帧、关键帧、尾帧 | 每类帧独立创建任务并保存 URL、状态和错误信息 | [ ] |
| G-04 | 生成分镜视频 | 需要已有分镜图或关键帧；创建视频任务，完成后视频 URL 写入当前镜头 | [ ] |
| G-05 | 在没有分镜图/关键帧时生成视频 | 操作被阻止，不产生视频任务 | [ ] |
| G-06 | 批量生成当前集的分镜图，再批量生成分镜视频 | 每个待处理镜头各有独立任务；已完成镜头不会被重复提交 | [ ] |
| G-07 | 任务失败后点击重试 | 旧失败记录保留；显式重试创建新的任务 ID，成功结果替换当前结果并写入历史 | [ ] |
| G-08 | 同一镜头生成多个版本并切换历史结果 | 历史图片/视频可查看和恢复；刷新后历史列表仍在 | [ ] |
| G-09 | 刷新页面或重新进入项目 | 运行中任务可继续同步；成功、失败、URL 和错误信息与服务端一致，不回退为初始状态 | [ ] |

## 6. 调用记录、资源和导出回归

| 编号 | 操作 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| R-01 | 完成一次剧本、资产提取、分镜提取、图片和视频生成 | 后台生成运营/调用记录能按“短剧”筛选到对应任务；项目、剧集、镜头和任务 ID 可追溯 | [ ] |
| R-02 | 刷新项目、重复打开页面、手动同步任务状态 | 只读取/同步现有任务，不重复创建上游任务；浏览器 Network 中没有额外重复生成请求 | [ ] |
| R-03 | 在有视频的集上打开“剪映导出”，填写合法草稿目录和版本 | 导出接口返回 ZIP 并可下载；ZIP 包含当前集已生成的视频素材 | [ ] |
| R-04 | 导出时不填写路径或当前集没有视频 | 前端阻止导出并提示原因，不创建导出任务 | [ ] |
| R-05 | 原有短剧入口、原有创作页和原有导出入口各走一遍 | 页面可打开，原有生成/导出能力不被短剧实验室改动破坏 | [ ] |

## 不纳入本次真实业务测试

以下功能当前代码明确是前端演示或占位，不应把“页面状态变化”当作业务通过：

- “一键全流程”弹窗：当前只展示执行模式、配置和推进状态，不创建生成任务、不保存设置、不消耗后台渠道额度。
- 团队协作与审批：当前不创建项目组、不发送通知、不上传附件，成员和审批记录不会持久化。
- AI 内容审核：当前显示的是本地演示报告/评分，不代表真实 AI 审核结果，也不应作为导出前的业务门禁验收。
- 入口页的公共角色库、场景库、道具库按钮：当前显示“开发中”占位内容。

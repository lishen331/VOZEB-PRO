# 音频交付与智能规划修复执行单

日期：2026-09-09。范围仅 Agent 音频相关链路；不修改渠道密钥、默认模型，不覆盖协作者文件。

1. 持久化的本地/OSS代理媒体必须使用授权同源路径，不暴露 Worker origin。
2. 音频任务只把规划出的朗读正文送给 TTS，不拼接视觉简报、Skill说明或引用元数据。
3. 取消/失败/无成功资产不得显示已生成；部分完成资产仍保留。
4. 智能规划先核对持久化失败事件，再用当前配置的真实智能请求验收；不能用手动直达代替，也不凭超时报错修改Schema。
5. 先失败测试再修复；定向测试、类型、Lint、构建；本地/上线证据分开记录，未部署不得报告线上修复。

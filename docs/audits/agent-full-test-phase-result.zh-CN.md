# 2026-09-09 Agent 全量测试阶段结果

## 自动化质量门禁

在当前工作区执行完整 Vitest：

- Test Files：837 passed，6 skipped（共843）
- Tests：4119 passed，31 skipped（共4150）
- 结论：自动化测试无失败。

输出中的 DATABASE_URL、上游 403、审计写入失败等均来自测试 fixture/隔离环境预期路径；Vitest 终态为通过，不能直接当作线上产品缺陷。线上真实问题仍以对抗性报告中的复现证据为准。

## 当前仍需线上执行的核心矩阵

全量自动化通过不等于真实上游和浏览器验收完成。以下线上用例仍缺真实证据：

1. 修复后重新验证参数页签污染：浏览视频/音频参数后发送纯文字，不得生成媒体任务。
2. 修复后重新验证图片盲测：必须读出图中真实错误码、英文和请求 ID，或明确无法读取；不能空承诺。
3. 相同文件名、不同图片内容的视觉输入对照。
4. 文生图、图生图、双参考图参数一致性。
5. 文生视频、图生视频、首尾帧和混合参考。
6. 五个 Skill 的快捷入口/菜单入口/移除/更换/冲突组合。
7. 文案→图片→视频依赖链和只重试失败子任务。
8. 提示词库、历史、灵感、上传竞态、430px/深色/软键盘。
9. 普通账号计费、权限、退款、轻量并发。
10. 正式音频模型仍为环境阻断。

## 下一步

先不要把“全量自动化通过”宣布为生产验收通过。下一阶段应集中修复 P0-01（模式污染）和 P0-02（视觉输入/空交付），然后一次性执行上述线上矩阵并产出最终发布门禁报告。

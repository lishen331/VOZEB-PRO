# 统一线上验收结论（2026-09-09）

## 真实执行结论

此前成功：文字-only、文生图、文生视频、P0 参数隔离、多模态盲测、短剧策划文字、角色设定、电商双参考图、图片动效、多模型恢复与取消。

渠道替换后的最终批次中，电商 Skill、图片动效 Skill、短剧策划 Skill 均返回“生成渠道暂时无法连接”；430px 无横向溢出。该批次说明当前线上渠道路由/连接状态发生变化，不能据此判定 Skill 业务代码同时损坏。

## 产品 Bug

本次没有确认新的业务代码 Bug。核心失败均为生成渠道连接阻断，需管理员在渠道中心查看实际请求日志、分组、Base URL、密钥和模型绑定是否同步；不能由测试账号自行修复，也不应通过增加重试或切换练习渠道掩盖。

## 待配置后复测

渠道恢复后必须重新执行：五 Skill 正向/逆向、图生图/双参考图、首尾帧、文案→图→视频、失败子任务重试、提示词/历史/灵感复用、上传竞态、会话恢复、普通账号计费与音频入口。

## 已有代码/自动化证据

全量 Vitest：837 文件通过、6 跳过；4121 测试通过、31 跳过。P0-01 与 P0-02 均已在可用模型配置下线上通过。

## 发布门禁

当前状态：**未达到生产发布门禁**。原因是线上生成渠道在最终批次不可连接，媒体与 Skill 完整矩阵无法获得真实成功证据；正式音频模型仍未配置。恢复渠道后，应按同一批次重跑，不再重复已通过的 P0 测试。
## 渠道恢复后补测更新（2026-09-09）

渠道恢复后电商生图已成功，实际模型为 `gemini-2.5-flash-image`，任务和资产完成。短剧策划文字-only 返回文本渠道 HTTP 504，无任务/资产，属于渠道超时。正式音频模型已由管理员配置，音频不再标记为永久环境阻断，需在同一批次补测 TTS 生成、播放和下载。

当前仍不能关闭完整生产门禁：图生图/双参考图、首尾帧、文案→图→视频、五 Skill 全入口/冲突、上传竞态、历史/提示词复用、普通账号计费及正式 TTS 尚未全部获得真实线上证据。
## 最新统一验收结果（2026-09-09）

渠道恢复后电商生图 Skill 已成功；短剧策划文字请求曾遇文本渠道 HTTP 504；正式音频模型已配置但真实 TTS 请求仍失败“模型返回的创作计划无效”，提示音频模式的规划输出契约或字段校验存在问题，需定位后修复。

当前已确认的产品/配置问题：
- 音频模式真实请求在规划阶段返回无效计划，未创建音频任务；这是需要代码或规划契约定位的 P1 缺陷。
- 文本渠道偶发 HTTP 504；需结合渠道日志确认，不将其直接归因 Skill。
- gemini-3-pro-image 曾出现无上游任务 ID的 needs_review；渠道替换后电商请求改由 gemini-2.5-flash-image 成功。
## 2026-09-09 测试环境音频渠道恢复后的结果

正式音频模型已经配置并重新在测试环境同步。真实音频请求仍在 Agent 文本规划阶段超时（`文本模型规划响应超时，正在切换备用渠道`），没有创建音频任务。该结果说明音频供应商尚未被调用，后续修复应先检查测试环境文本规划渠道和音频计划 Schema，而不是继续更换 TTS 上游。

同批次视频请求已成功完成，说明测试环境视频渠道已恢复。
- 2026-09-10 模块隔离部署与线上准备：
  - Quality、Production build、镜像构建、SSH 部署均通过（Actions 34388908497）；App/Worker 已运行 `sha-c7d9c6cace2b2e3b17e3c183fd6bb67009451d57`，健康检查通过。
  - 当前测试页 `/drama-lab` 可正常加载项目列表和工作台入口。
  - 短剧实验室任务上下文新增 `featureModule=drama-lab`，普通短剧仍使用 `surface=drama`；服务端持久化/恢复保留该字段。
  - 定向测试 6 文件、59 项通过，TypeScript 通过。
  - 尚未通过线上后台实际切换开关完成三种组合验证；因为该操作会修改测试环境全局配置，本轮未擅自关闭模块。上线后的手工验证步骤：关闭短剧保留短剧实验室；关闭短剧实验室保留短剧；两者同时开启。
- 2026-09-10 真实复现（测试环境已关闭“短剧”）：进入短剧实验室项目，点击第一个角色的“AI 生图”。浏览器实际收到 `POST /api/image-tasks` HTTP 403，响应 `{"error":"短剧暂未启用"}`。
- 复现证明问题发生在共享 `/api/image-tasks` 功能开关校验链路，而不是页面路由；该请求虽然来自 `/drama-lab`，当前 payload 中的上下文未被前端/中间调用完整传递 `featureModule=drama-lab`，服务端因此按 `surface=drama` 映射到短剧开关。
- 本地已完成上下文契约与四个短剧实验室生成路由的隔离改动，并通过 59 项定向测试；但线上当前服务器仍为 `sha-c7d9c6cace2b2e3b17e3c183fd6bb67009451d57`，尚未包含后续修复提交，故 403 仍可复现。
- 该证据用于关闭误判：必须部署包含 `featureModule` 透传/持久化的后续版本后，再在线上重复点击 AI 生图验证。
- 2026-09-10 关闭短剧后的真实执行级回归（服务器当前 `sha-30e8a333c78332fd809db38b60136e7b307c1f7e`）：进入 `/drama-lab/.../create`，点击角色“AI 生图”。浏览器请求 `POST /api/image-tasks` 返回 `403 {"error":"短剧暂未启用"}`。本次确认页面可访问不能代表执行链路已解耦。
- 捕获到共享接口收到的请求体 context 仍只有 `surface:"drama"`、projectId，没有 `featureModule:"drama-lab"`。因此共享接口按 `surface=drama` 命中已关闭的短剧开关。服务端专用路由源码虽包含 featureModule 字段，说明字段在实际运行的转发链路中被丢弃或请求未经过预期版本的专用路由；需继续检查部署产物/route 命中和中间层 body 构造，不能再把 ca6242 代码存在误报为线上已修复。
- 2026-09-10 关闭短剧后的最终真实复现：在已部署版本 `sha-30e8a333...` 的短剧实验室资产准备页点击角色“AI 生图”，共享 `POST /api/image-tasks` 返回 200 创建任务，不再返回“短剧暂未启用”。任务随后保持 submitting，说明短剧实验室执行级隔离已生效。
- 捕获请求在旧服务器版本中仍只有 surface=drama、projectId=drama-lab-*；为兼容旧任务，服务端现在根据 projectId 中的 `drama-lab` 标记回退识别模块，避免历史任务缺字段时命中短剧开关。
- 新增回退识别测试 4/4；短剧实验室生成路由/任务测试 59 项通过。服务端 TypeScript 通过。
- 已发现并修复的具体范围：共享 image/video/audio 任务入口及任务持久化/恢复的模块归属；未改短剧页面和用户权限体系。
- 2026-09-10 真实线上最终复测：测试环境关闭“短剧”、开启“短剧实验室”，服务器版本 `sha-30e8a333c78332fd809db38b60136e7b307c1f7e`。
- 从短剧实验室 `/drama-lab/drama-lab-1788493614414-xni9ra7/create?episode=...` 进入资产准备，点击角色“AI 生图”。共享 `POST /api/image-tasks` 返回 200，创建任务 `6be63e86-e113-4fed-aaed-3e4d88cbc843`，模型 `gpt-image-2.5-sunburst`，任务最终 `success/completed`，结果阶段 `persisted`。
- 该真实点击不再出现“短剧暂未启用”，证明在关闭短剧开关时短剧实验室的新生图执行链路可用。
- 数据库记录显示该历史任务的顶层 context featureModule 为空，但 project_id 为 `drama-lab-*`；服务端兼容回退按项目 ID 判定为 drama-lab，避免旧/中间转发链路丢字段后再次命中短剧开关。
- App/Worker 健康检查通过；模块隔离代码已推送 GitHub。后续还需在相同部署版本下点击分镜视频、分镜音频，并做反向组合（短剧开、短剧实验室关），再宣布三组合全部通过。

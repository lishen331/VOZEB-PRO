# Agent 主线专项测试汇总（2026-09-10）

## 总结

本轮已按专项分工完成可安全执行的线上审查，但**没有完成全部生产测试矩阵**。原因不是测试人员遗漏，而是当前 `/create` 页面自动恢复已有会话，点击“新建对话”后无法确认生成空白会话；媒体代理因此没有发起新的收费生成，避免污染历史任务和重复扣费。

## 已完成的专项证据

### UI / 历史 / 提示词

报告：`docs/audits/delegated/ui-history-matrix-2026-09-10.md`（子代理已生成；若当前工作树缺失，保留其摘要并在下一次同步补入）。

已通过：
- `/prompts` 可访问；公共提示词数量 963；分页、跳页和末页 61 可用；
- 公共提示词复制、加入我的素材；
- `/my-prompts` 可访问；个人提示词复制、加入素材、删除入口；
- 深色主题入口；
- 390×844：scrollWidth=390、innerWidth=390；`/create` 可打开；
- 页面基础接口和控制台无新增错误。

未执行：最近生成引用、历史分页/批量管理、会话切换/刷新恢复、取消/重试/重新检查、430px 完整弹窗/软键盘、剪贴板读取、提示词直接插入 Agent。

### 权限 / 模块 / OSS

报告：`docs/audits/delegated/permissions-modules-matrix-2026-09-10.md`。

已通过：
- 创意工坊入口、项目列表、项目详情、协作和审批接口返回 200；
- 角色/场景/道具、分镜工作台和生图/生视频/音频入口可见；
- 参考图/生成图经服务端跳转到 OSS 签名地址并成功读取；
- 当前数据库同时存在 `drama-lab-*` 与 `drama-*` 项目 ID。

未执行：插件开关四组合、成员/非成员权限、邀请/移除/主动退出、积分/幂等/取消退款/失败退款、轻量并发。

### 媒体生成

报告：`docs/audits/delegated/media-matrix-2026-09-10.md`。

已通过前置：登录、Agent 页面、模型/Skill/会话/资产接口、历史 OSS 资源读取；控制台 Errors=0、Warnings=0。

未执行：文生图、图生图、双参考图、文生视频、图生视频、首尾帧、TTS 播放下载。测试前置阻断为无法确认新建空白会话。

## 已有历史线上通过证据

此前已经有独立真实证据：P0-01 参数页签隔离、P0-02 多模态盲测、文生图、文生视频、角色设定、电商 Skill、短剧策划文字、双模型、图片动效、任务恢复/取消/重试、移动 390/430px，以及创意工坊在关闭短剧时生图成功。

## 当前产品/环境问题

1. **测试可执行性问题**：`/create` 自动恢复会话；“新建对话”无法确认空白会话，导致媒体矩阵无法安全执行。
2. **历史模块状态兼容**：部分旧任务缺模块标识，需要按项目来源恢复；新代码已覆盖显式标识和历史兼容。
3. **上游/配置问题**：此前出现文本 HTTP 504、无上游 task ID、模型不可用；需按同一请求时间和请求 ID 对账，不能归因到前端。
4. **音频 UI 交付问题**：代码已修复内部地址/下载/取消成功文案，但还需部署后使用真实 TTS 回归。

## 结论

当前状态：**核心路径和高风险问题已有通过证据；专项审查已完成；全量生产矩阵未完成。**

要真正完成全量测试，需要先解决“创建可验证空白会话”这一测试前置，或提供专用隔离测试账号/会话创建入口；之后三个专项代理可继续执行未完成清单，主规划师再生成最终发布报告。

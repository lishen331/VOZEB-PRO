# Agent 高优先级问题修复与回归记录

## 已确认需求

先修复 /create 参数页签污染生成模式、截图分析只承诺不交付，再完成图片、视频、音频、五个Skill、多模型和恢复链路验收。保持普通画布与短剧业务不做无关改动，不更改平台账号或上游默认配置。

## 原始线上证据

见 agent-create-live-adversarial-review.zh-CN.md。模式污染通过同文本对照复现；报错截图内容为HTTP 403 / TEST_PERMISSION_DENIED / QA-7429，旧版本没有给出识别内容。

## 本次修复

1. Agent模式下浏览/编辑参数不设定强制产物mode，提交时清除残留隐藏mode；显式图片/视频/音频模式保持有效。选择首帧/首尾帧为明确视频操作，同时更新可见的模式标签。
2. 规划前读取已验证用户归属的真实媒体；内部媒体采用已有签名能力供Worker读取，转OSS时不向外转发Cookie/Worker认证。读取失败不允许假装看过图片。限制沿用上传大小、系统代理请求大小与文本超时配置。
3. 媒体与JSON文字上下文分开：Chat使用媒体content parts、Responses使用input parts、Gemini使用inlineData；结构化修复仍保留媒体。GlobalAiOpc的Gemini/Claude适配保存图片内容。自定义协议缺少多模态输入契约明确失败，不静默丢图片。
4. 对话型图片识别提示词要求直接回答实际识别内容，不以“我会分析”替代成品。最终是否符合目标仍需原图盲测，不以提示词或completed状态作为通过证据。

## 验证进度

- 新增失败测试先复现：参数隐藏mode、Chat/Responses/Gemini媒体缺失、结构修复丢媒体、GlobalAiOpc适配丢媒体。
- 114项定向测试通过；覆盖媒体读取失败、大小限制、协议相对URL拦截、Cookie不外发、Worker签名、调用方接线、协议转换等。
- 首轮全量：819测试文件通过、6跳过；3963项通过、31跳过。后续新增4项定向测试另已通过，全量最终数字以部署CI为准。
- TypeScript通过；全量Lint通过；Prettier通过；生产构建通过。
- 本地旧依赖目录有空包，恢复相同版本的缺失文件后测试可运行；旧Next类型缓存引用已删除IP路由，旧构建产物进入Lint，均归档到忽略的调试目录，未用修改规则跳过业务检查。
- 本地生产构建浏览器可打开登录页，但laoshi1凭据不适用于本地，未修改用户密码。线上复测待修复部署后进行。
- 开发地图和接口索引验证通过：341 routes / 52 pages / 117 tables。整合同事更新后必须重新执行。

## 后续验收顺序

1. 线上模式切换原失败用例与文字对照。
2. 真实报错截图盲测、多轮追问、读取失败。
3. 单图/参考图/双参考图，五个Skill，音频，单模型/多模型。
4. 视频额度具备后文生视频/图生视频/首尾帧；按实际供应商能力测试视频输入，不承诺所有文本模型接受视频。
5. 完成、失败、取消、重试、刷新和离页恢复，核对任务及计费记录。

状态：修复和本地质量门禁完成，线上部署及完整媒体验收未完成，不得记为全量通过。

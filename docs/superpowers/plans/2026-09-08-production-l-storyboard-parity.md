# 生产版 L → V 制作工作台行为等价迁移计划

日期：2026-09-08。状态：进行中，不代表整体迁移完成。

## 已批准目标与边界

保留服务器生产版 L 的功能、按钮布局、操作顺序、默认行为、反馈和生成逻辑，融入 V 的 React/TypeScript、权限、算力、任务、日志及 OSS。V 保留团队协作、审批等独有能力，不改变已对齐的业务权限。首页和详情页只回归衔接，不重新设计。不修改普通短剧、普通画布与 Agent。

不以同名按钮、HTTP 200、代码中存在函数、CI 通过认定业务等价。不要求随机生成图片像素相同；同上游画面不同，需要比较提示词、模型、参数、参考图与后处理，不能武断归因。

## 基准证据

- 生产 L 监听 19080，进程工作目录 `/home/lsgmadminuser/workspace/local-mini-drama-20260805/backend-node`。
- 生产 L Git 基线 `34739d3d4c3de8e892941facb11fb405fac7f773`，存在大量未提交生产定制；本地 L 为 `7b6c1a748e9e3013b88a902cfbfd31ec283da0d1`。不能只用任一提交表示生产全部代码。
- 196 个源码/迁移/构建入口文件已复制到仓库外 `.superpowers/l-production-reference-20260908`，逐文件 SHA256 写入 manifest.json。排除配置密钥、数据库、日志、备份。生产服务未写入或重启。
- 前端构建入口引用 `index-CyAwCCl5.js`；源码和运行产物对应性尚需进一步核验。
- 生产前端风格表 39 项，与 V 风格表的中英文正文逐项一致。生产后端回退表为 32 项，另外 7 项依赖前端写入的完整风格元数据，不得误删。
- 生产 `utils/dramaStyleMerge.js` 优先读取 metadata.style_prompt_zh/en，否则展开预设；V 原先仅传 project.style，帧及拆镜请求未注入已迁入的完整风格描述。

## 阶段与验收任务

### 1. 生产行为矩阵
- [x] 定位运行目录并固定生产源码只读快照。
- [x] 核对前后端风格表与 V 原文差异。
- [ ] 核对实际构建版本、有效提示词覆盖与生产定制的完整清单。
- [ ] 逐项记录：操作→前置条件→参数→请求→保存→反馈→V 实现→验收证据。
- [ ] 分类：已等价、行为不同、有代码未接入、缺失、待验证。

### 2. 生成契约（先完成可独立验证的风格展开）
文件：web/src/lib/drama-lab-style-prompt.ts 与测试；短剧实验室 frame/shot/storyboard/asset 服务及对应测试。
- [x] 先写失败回归，证明拆镜实际请求缺失完整风格正文。
- [ ] 预设值展开中英文原文，自定义保留，不猜测“电影感国漫”，空值不套假默认。
- [ ] 接入真实提示词构造，保持原 style 值与普通短剧行为不变。
- [ ] 核对九套模板、生产 DB 覆盖、动态注入、锁定契约、帧清洗和参考图顺序；不覆盖 V 管理员模板。
- [ ] 同输入 fixture 请求对比；少量真实调用另记，不混淆测试结论。

### 3. 数据与编辑
文件：drama-project-contract、drama-project-service、实验室制作页与对应路由/服务测试。
- [ ] 富字段样本验证编辑、保存、切集和刷新不丢字段。
- [ ] 核对 classic/universal、首尾帧、数量/总时长、旁白与序列图的配置、持久化和生成生效。
- [ ] 核对资产绑定、编辑、排序、增删；保留已有 ID 和结果。

### 4. 布局与使用习惯
- [ ] 以生产 L 桌面/窄屏为基准，保留按钮分组与顺序、参考资产和视频位置。
- [ ] 同一镜头可查看编辑提示词、参考图、图片和视频，不额外拆页。
- [ ] 保留 V 单集作用域、定位、团队协作和审批；协作面板可收起，不挤占主流程。


### 4.1 制作工作台与本集画布切换（2026-09-08 补充确认）

- 画布切换明确纳入迁移及回归范围。借鉴 L 的切换按钮位置、导航排版和使用习惯，但不迁移 L 的 Canvas 引擎，不用简化画布替换 V。
- 保留 V 当前实验室画布切换链路：`/drama-lab/:projectId/canvas?episodeId=...` → `episode-canvas` 绑定服务 → `/drama-canvas/:canvasId`，使用 V 现有原生画布能力及实验室适配层。
- 一集一个稳定绑定的画布，不是整个短剧项目共用一个画布；反复切换不应重复创建。
- 保留 V 原生画布现有的节点、连线、素材、生成、编辑及其他可用能力；不以迁移 L 为理由裁剪这些能力。
- 返回制作工作台应保留当前项目、当前集；带镜头定位参数时核对返回后的镜头定位。不能返回错集或直接退到项目列表。
- 核对切换前未保存编辑、任务执行中切换、刷新恢复与回写边界；不因切换丢稿、重复生成、覆盖镜头或丢失画布自由编辑内容。
- 沿用当前 V 的显式回写/同步规则，本项不批准新增双向自动覆盖，也不改普通画布架构。
- 当前已核对源码路由，不代表全部切换行为已验收。

验收任务：
- [ ] L/V 切换入口位置、排版、返回路径对照。
- [ ] 同集重复进入复用画布 ID，不同集对应不同画布。
- [ ] 带项目/集/镜头定位往返，刷新后仍保持绑定。
- [ ] 编辑中和任务执行中切换，无丢失、重复提交或错误回写。
- [ ] V 原生画布已有能力回归通过，普通画布不受影响。

### 5. 批量与任务
- [ ] 核对全选范围、选中项执行、跳过已完成、失败重试、取消停止后续语义。
- [ ] 任务立即显示；刷新恢复；未知状态检查原任务；无重复任务与计费。
- [ ] 部分成功保留；前台、后台和上游状态一致。

### 6. 验收与发布
- [ ] 每条链路相关自动测试、类型检查、独立浏览器会话测试；测试项目不覆盖用户制作数据。
- [ ] 图片实际加载、参数生效、刷新持久化及后续可用；禁止只看 DOM。
- [ ] 代码独立提交，不夹带同事改动；推送前更新并验证开发地图，不用空提交打断流水线。
- [ ] 完整门禁、自动部署、运行镜像、浏览器回归均有证据后才报告上线。

## 当前缺口

本轮仅开始第一条生成契约修复；完整 UI、批量编排、有效模板覆盖及真实上游图像比较尚未完成。历史报告中“全部迁移完成”不能作为本次验收依据。
## 第一批进度（2026-09-08）

- 通过只读 SQLite 查询确认：生产 L 当前语言 zh，prompt_overrides 表为空；九套默认正文与锁定部分已提取到仓库外参考快照。未复制任何渠道密钥或生产项目数据。
- 风格展开已接入实验室资产提取、分镜拆解、三类帧文本规划、镜头图片/视频准备上下文，保留原 style 值与参考图顺序。
- 验证：先观察拆镜请求测试因缺少 stylePromptZh/stylePromptEn 失败，再实现；相关 4 个测试文件 62 个用例通过；全项目 TypeScript 检查通过；本批相关 ESLint 通过。
- 这只是第一条生成契约修复，不代表有效提示词已全部对齐。未改变后台模板，未重写数据库，未进行本批真实上游生成或部署。
- 下一步：核对九套正文与动态合同；增加三类帧实际文本请求 fixture；然后完成镜头操作矩阵和布局适配。UI 不提前套壳。
## 第二批进度：三类帧实际运行提示词

- 三类帧默认正文已按服务器 L 实际 getter 返回值迁入（不是后台显示的简版），保留许可与 SHA256 回归。
- 已接入服务端声明变量替换，并通过真实本地 TCP fixture 验证三类帧、自定义覆盖、连续性、清洗和请求前拦截。
- 详细证据：[三类帧提示词迁移记录](../../audits/2026-09-08-production-l-frame-prompt-parity.md)。相关 71 用例及全量 4024 用例通过，TypeScript 通过。
- 剩余六套模板不能标记完成；资产提取 schema 会排除 L 的 appearance/image_prompt，是下一批必须一起解决的生成契约和持久化缺口。
- 本批未部署或真实上游验收；布局/批量/画布往返任务保持待办。
## 第三批进度：资产提取字段贯通（2026-09-08）

- 角色/场景/道具三套默认提示词已按生产 L 实际 getter 返回值迁入，并以哈希测试锁定原文；服务端展开风格与画幅变量，管理员覆盖继续优先。
- 提取工具声明 appearance、imagePrompt、role、type、time；L 的 location/prompt/image_prompt 在实验室边界映射到 V 的 name/imagePrompt，不丢弃视觉描述。
- 前端接收提取结果时保留视觉字段；角色编辑中可查看修改外貌，资产编辑中可查看修改生图提示词；场景重新加载保留 imagePrompt。
- 复用 V 的 project_json 持久化，公共资产标准化器仅增加可选字段保留，不改普通短剧 UI、画布或既有权限，不新增数据表。服务层 JSON 保存/恢复往返测试通过；尚未做真实 PostgreSQL/浏览器刷新验证。
- 资产生图优先使用提取或编辑的 imagePrompt；角色补充 appearance；场景/道具有专门提示词时不重新注入含人名的背景故事，避免把道具归属者画入主体图。
- 素材库保存/导入保留上述视觉元数据（仅声明字段），不复制任意字段。
- 先红后绿：提取字段、工具 Schema、保存恢复测试共 5 个失败点复现后修复。相关测试 42 用例通过，另新增实际请求模板替换测试 1 个通过。
- 全量测试（新增最后一条测试前）824 文件通过、6 跳过，4036 用例通过、31 跳过；最后新增请求测试所在文件 6 用例通过。TypeScript、全量 ESLint、全量 Prettier 通过。
- 尚待：真实 TCP 资产调用与 PostgreSQL/浏览器闭环；同地点不同时段的去重语义；故事扩写及分镜 system/user suffix 剩余三套模板；资产生成专业润色；完整布局/批量/画布切换。
- 本批未推送部署，没有使用真实上游额度。开发地图脚本仍受此前 PowerShell 7 不可用的环境问题影响，推送门禁未宣称通过。
## 第四批进度：故事与分镜剩余三套默认模板

- 生产故事扩写、分镜 system、分镜 user suffix 原文已迁入并有 SHA256 对照。
- 修复多集正文写死一集；单集兼容服务仍为一集。固定输出契约明确 L → V 字段与 ID 映射，声音设计保留到 videoPrompt。
- 角色外貌、场景时间及场景/道具图像提示词进入拆镜上下文。
- 详细说明：[故事与分镜模板迁移记录](../../audits/2026-09-08-production-l-story-prompt-parity.md)。全量 4044 用例通过，31 跳过；TypeScript 通过。
- 九套默认正文都有实际生产来源，不代表各模式动态后缀、真实上游质量、工作台布局和画布切换均已完成。
- 下一阶段：生产分镜配置矩阵（模式、数量、时长、旁白、序列图）→ V 参数契约/保存/调用，再做对应的工作台按钮布局。保持 V 原生画布和团队协作。
- 本批仅本地提交，不推送部署；开发地图门禁环境问题与浏览器回归仍需解决。
## 第五批进度：分镜数量与总时长目标（2026-09-08）

- 新增当前集的分镜数量、视频总时长输入。留空交由模型判断；数量要求正整数，总时长允许正小数；非法类型/零/负数/非有限值在创建任务前拒绝。
- UI 草稿按集分开保存于组件内存，不串集；本批不宣称未提交草稿刷新后会恢复。
- 提交后随 workflow.options.storyboardOptions 持久化到原任务，advance 和取消后恢复沿用原参数，不使用页面当前值替换任务快照。
- 已有活跃任务与新请求的数量/时长不同，返回冲突而不是假装接受新配置。同请求 ID 重试仍保持原幂等语义。
- 分镜 system prompt 和结构化上下文都传入目标，沿用生产 L 的数量 ±20%、总时长 ±10% 软约束；不硬截断输出，也不凭空写死最大数量。模型最终偏差的结果提示尚待后续验收补充。
- API 接口不变，extract-storyboards 请求新增可选 storyboardOptions 对象（shotCount/totalDuration），workflow 通用 options 保留同一字段。旧任务字段缺省仍可恢复。
- 先红后绿：API 丢参数、任务丢配置及校验缺失已复现后修复。相关 5 个文件 43 用例通过；随后补充取消/恢复参数断言，workflow 测试 20 用例通过。
- 全量测试 827 文件通过、6 跳过，4066 用例通过、31 跳过；TypeScript 通过；全量 ESLint 通过。本批 12 个代码/测试文件 Prettier 检查通过。
- 全量 Prettier 未通过：17 个其他 Agent 正在修改的创作/鉴权/任务文件存在格式差异，没有格式化或提交这些无关文件。
- 未完成：浏览器交互与真实上游验收、项目级配置默认值、classic/universal、首尾帧模式、旁白及序列图配置；V 原生画布切换和团队协作保持不变。
- 本批仅本地提交；不以控件和单测通过宣称全计划或线上已完成。开发地图脚本的 PowerShell 7 环境门禁仍需修复后再推送。
## 第六批进度：经典/全能与解说旁白（2026-09-08）

- 新增经典/全能单选和解说旁白勾选；客户端默认经典且不自动扩写旁白。每集草稿隔离，已提交参数随任务保存，恢复沿用。
- 迁入生产 L getStoryboardUniversalOmniModeSuffix/getStoryboardNarrationExtraInstructions 实际原文到 drama-lab-production-storyboard-modes.json，沿用既有 MIT 许可。固定契约明确 creation_mode/universal_segment_text 映射到 V 的字段。
- 返回结果在检查点保存前校验请求模式：请求全能却返回经典、全能缺少多行文本、已开启旁白却为空均拒绝；流式及续写使用同一验证入口。未开启额外旁白不删除剧本原有旁白。
- 本批只验证全能标记和非空多行结构，未严格验证子分镜条数、每段时长之和、参考图编号对应、对白逐字保留等完整合同，不宣称全能格式完全等价。
- 七个失败回归先复现后修复；最终相关 5 文件 54 用例通过，TypeScript 通过；全量测试在并行协作合并前为 829 文件通过、6 跳过，4086 用例通过、31 跳过。本批 ESLint/Prettier 通过。
- Playwright 独立本地组件测试：加载真实 React 控件和参数标准化函数，桌面选择全能+旁白后输出 creationMode=universal/generateNarration=true；390px 切回经典并取消旁白，输出 classic/false。数量 12、总时长 90.5 均保持。此测试未加载 V 全站样式，不作为正式工作台布局或端到端生图验收。初次 Vite 扫描超时与 JSX runtime 导出问题已在临时测试宿主中修正；未修改项目依赖，测试服务已停止。
- 尚待：全能提示词在视频生成请求中的选择与参考图编号匹配、子分镜时长完整校验、项目配置刷新恢复、首尾帧开关/序列图、生产浏览器与真实上游质量验收。
- 生产 L 的续写函数还存在旧“单行”格式说明，与新全能后缀不同；不照搬这个冲突，V 续写仍使用本次多行模式及验证。
- 不修改团队审批与 V 原生画布；本批未部署，不标记全计划完成。
## 第七批进度：全能分镜视频入口接通（2026-09-08）

- 全能模式视频服务端现在按场景 → 角色 → 道具 → 可选分镜图的稳定顺序收集主参考图，保留编号与快照；不再把场景误标为首帧，也不静默截断参考图。
- 全能提示词在创建任务前校验 1–8 个连续子分镜、正时长、总时长一致、`@图片N` 不越界且禁止 `@人物N`；失败返回可操作错误。
- 单个生成和批量生成前端不再要求全能模式必须先有分镜图；经典模式保持原有关键帧/分镜图前置条件。全能模式编辑 `universalSegmentText`，经典模式继续编辑 `videoPrompt`。
- 工作流视频步骤同样允许全能模式在无分镜图时进入正常生成路由；经典模式约束不变。
- 生成视频 route 回归覆盖实际请求体：有序 references、全能 prompt、frameSnapshot 和仅保存 `universalSegmentText`；相关 route/service/workflow 测试通过，TypeScript 通过。
- 当前仍未完成：真实上游/生产浏览器验收；自定义渠道是否声明多图参考能力仍按模板显式判断，未确认的渠道会拒绝全能模式而不是静默降级。
- 本批未提交、未推送、未部署；不得据此宣称全计划完成。

### 浏览器验证记录（2026-09-09）

- 已启动本地 Next.js + generation worker，入口为 `http://localhost:3000`。
- Playwright 已验证首页加载、短剧制作入口可点击，并确认受保护的短剧工作台会跳转 `/login?next=%2Fdrama`。
- 当前本地浏览器会话没有已登录的测试账号，因此无法进入具体项目/分镜工作台执行真实生成点击；没有伪造成功结论。
- 真实工作台回归需使用测试账号登录后继续：经典视频、全能视频（无分镜图）、批量生成、刷新恢复和错误提示。

### 测试环境部署门禁记录（2026-09-09）

- 已确认测试环境 `http://8.163.37.148:3001` 当前仍为旧部署版本；本批全能视频改动尚未推送，因此不能用旧环境判断本批结果。
- 按仓库门禁执行开发地图更新与验证时，当前机器仅有 Windows PowerShell 5.1，`pwsh` 不存在；直接运行脚本时中文字符串被错误解码并产生 PowerShell 解析错误。
- 失败位置为 `过程文件/更新开发地图.ps1` 与 `过程文件/验证开发文档.ps1` 的脚本解析，不是业务测试失败。未跳过门禁、未推送、未部署。
- 本批自动化回归仍为 4 个相关测试文件、52 个用例通过，TypeScript 通过；等待修复 PowerShell 7/脚本编码门禁后再提交推送。

### 测试环境部署与浏览器验证（2026-09-09）

- PowerShell 7.6.5 便携版已从 `C:\Users\admin\Downloads\PowerShell-7.6.5-win-x64.zip` 解压到仓库外 `C:\Users\admin\AppData\Local\PowerShell\7.6.5`，`pwsh --version` 验证通过。
- 开发地图/接口索引门禁通过：343 routes、52 pages、117 tables，索引与源码一致。
- 本批提交：`56436608`；与远端同事提交整合后 HEAD：`94463c4a`。
- GitHub Actions Staging image run `34271914988` 成功：源码检查、生产构建、镜像构建推送、SSH 部署均通过；`origin/develop` 已指向 `94463c4a`。
- Playwright 已登录测试环境 `http://8.163.37.148:3001`，进入 `/drama-lab`、项目大纲和分镜工作台；确认部署后的工作台存在一集 11 个分镜、任务状态栏、画布入口、资产引用和团队协作入口。
- 对经典模式镜头点击“生成分镜视频”时，按预期显示“请先生成当前镜头的关键帧或分镜图”；这是经典模式既有前置约束，不代表部署失败。该测试项目没有全能模式镜头，因此尚未验证全能无分镜图路径。
- 未消费真实上游视频额度；当前可继续用已有全能模式项目或先创建全能镜头做针对性验证。

### 全能模式真实上游验证（2026-09-09）

- 测试项目：`drama-03tgsyBr88SYFPAWX_Iz7`，第 1 集第 1 镜头。
- 已通过 API 将该镜头切换为 `creationMode=universal`，保留真实场景、角色、3 个道具及主参考图，写入合法 1 子分镜/4 秒 `universalSegmentText`。
- 前端点击“生成分镜视频”后显示“分镜视频任务已提交”，服务端持久化状态为 `running`，任务 ID `22b9750a-11ca-44f5-98b9-013e614e003c`。
- 上游实际任务 ID：`task_GXaY65jjArQa3mVZgSPgAEgqKFJUWBgd`，模型 `doubao-seedance-2-0`。
- 上游最终返回 `success`，4 秒 MP4；调用 sync-generation 后镜头持久化为 `generationStatus=success` 并写入视频 URL `/api/reference-assets/permanent/2026/09/08/videos/20260908-203054-942de81e-94ee-47cf-b80b-5279456a68e8.mp4`。
- 刷新后页面回到了剧本 Tab，因此页面 DOM 未显示 video 元素；这是当前 Tab 展示状态，不是任务结果丢失。API 查询已确认视频 URL 持久化成功。

### 第八批进度：批量任务可见取消入口（2026-09-09）

- 批量图像/视频执行原本已有 AbortController 和批量等待器，但工作台没有可见的取消按钮，用户无法按 L 的习惯主动停止批量流程。
- 在批量按钮组旁新增“取消批量任务”；仅在图像或视频批量运行时显示，点击会中止后续批量提交与等待流程。
- 已提交到服务端的子任务不被伪造取消，仍保留其任务 ID，由已有同步/恢复链路继续处理；这避免把“停止后续提交”误报成“撤销上游已提交任务”。
- 新增源码回归覆盖取消入口和已有帧控制；相关测试 3/3 通过，TypeScript 通过，Prettier 与 diff 检查通过。
- 尚待：测试环境浏览器验证批量取消、部分成功和刷新恢复；本批未提交、未推送、未部署。

### 第八批补充：批量前置校验与阶段收尾（2026-09-09）

- 测试环境实测发现，批量生成遇到缺少资产参考图时会逐镜头触发弹窗，造成连续打断。现改为批量开始前统一汇总缺失资产，一次提示并跳过不可执行镜头；仍有可执行镜头时继续批量。
- 已提交镜头仍由服务端任务状态和同步链路负责，不被前端批量取消伪造为已撤销。
- 取消批量任务入口、缺失资产汇总和现有批量等待器相关回归已通过；TypeScript 通过，格式检查通过。
- 测试环境当前已验证批量按钮和缺失资产提示行为；待本批新版本部署后再次确认最终页面。

### 第八批最终部署与浏览器验收（2026-09-09）

- 第八批最终提交 `37fb9a96` 已推送 `develop`；此前远端协作者提交已先合并，GitHub Actions Run `34299987285` 成功完成源码检查、生产构建、镜像推送和 SSH 部署。
- 测试环境实际验证批量入口：点击“批量生成分镜图”不会再逐镜头弹出 11 次窗口，而是一次性显示缺失资产汇总并跳过不可执行镜头。
- 当前测试项目的 11 个镜头均缺少场景/道具参考图，因此本次浏览器证据覆盖“统一前置校验/跳过”和按钮展示；未虚构部分成功混合批量结果、取消中途提交或真实批量视频额度结果。
- 相关批量等待器、取消入口和前置校验测试 12/12 通过，TypeScript、Prettier、diff 检查通过。
- 第八批已部署并验证的范围：取消入口、停止后续提交语义、缺失资产统一提示、已提交任务仍由服务端同步。剩余阶段性验收缺口是使用带完整资产的测试镜头验证取消中途和部分成功混合结果。

### 分镜工作台闪烁修复（2026-09-09）

- 复现项目：短剧实验室「xxx」项目 `drama-03tgsyBr88SYFPAWX_Iz7`。
- 根因：分镜工作台的视频任务恢复 effect 在重新渲染时执行清理，清理函数删除 `pending` 状态；由于 `onReload` 等依赖变化，effect 再次启动，同一剧集重复请求 `recover-generation`、项目详情和任务列表，造成页面闪烁。
- 修复：新增 `recoveryStartedRef`，按 `projectId:episodeId` 记录恢复是否已启动；同一项目/剧集在当前挂载周期内只启动一次。清理时仅取消请求和清理 in-flight 引用，不删除启动标记。
- 切换不同剧集使用不同 key；任务恢复失败仍保留分镜卡片手动同步入口，不进入无限自动重试。
- 本地验证：相关分镜控制与批量测试 12/12 通过，TypeScript 通过，Prettier/diff 检查通过；部署后需在测试环境确认同一剧集点击分镜只发起一次恢复请求。

### 分镜工作台闪烁根因修复（2026-09-09）

- 测试环境复现：点击「xxx」项目制作页的「分镜工作台」后，5 秒内重复请求 `recover-generation` 约 34 次，并伴随项目/任务数据刷新。
- 根因：恢复 effect 的依赖包含 `onReload`、`messageApi` 等父层回调引用；恢复后触发项目刷新，父组件重新创建回调，effect 清理并重新启动，形成恢复请求循环。
- 修复：使用 `recoveryReloadRef` / `recoveryMessageRef` 保存最新回调，恢复 effect 只依赖 `project.id` 与 `episodeId`；结合 `recoveryStartedRef`，同一项目/剧集挂载周期只启动一次。
- 恢复失败仍保留手动同步入口；切换到其他剧集使用新的 key，不影响不同剧集恢复。
- 本地回归：分镜控制测试 4/4 通过，批量等待器测试 9/9 通过，TypeScript 和 Prettier 通过。
- 本次修复需重新部署到测试环境后再确认重复请求已消失。

### 分镜闪烁第二次修复（2026-09-09）

- 首次修复仅在 `StoryboardPanel` 内保存恢复去重状态；实际页面存在父层刷新导致子组件重挂载，组件级 ref 会被重置，测试环境仍观测到 5 秒约 32 次恢复请求。
- 第二次修复将恢复启动 key 提升到模块级集合 `dramaLabRecoveryStarted`，按项目和剧集维度跨组件重挂载去重；同时保留最新回调 ref，避免回调依赖变化重新触发 effect。
- 本地相关分镜控制测试 4/4 通过，TypeScript 通过，Prettier/diff 检查通过；待部署后复测同一剧集点击分镜只产生一次恢复请求。

### 分镜工作台闪烁最终验证（2026-09-09）

- 第二次修复提交 `65c06f33` 已推送并由 GitHub Actions Run `34312175884` 成功部署到测试环境。
- 使用测试账号进入项目 `drama-03tgsyBr88SYFPAWX_Iz7` 第 1 集，点击“分镜工作台”后等待 5 秒，`recover-generation` 请求数为 **1**，不再出现之前 5 秒约 32–34 次的重复请求。
- 页面正常显示分镜列表、任务状态、资产信息和生成控件；闪烁/反复刷新根因已验证消失。
- 根因修复包括：模块级项目/剧集恢复去重、回调 ref 隔离、恢复 effect 依赖收敛。

### 画布切换闭环阶段验收（2026-09-09）

- 测试环境项目：`drama-03tgsyBr88SYFPAWX_Iz7`，第 1 集。
- 从短剧分镜工作台打开本集画布后，服务端返回稳定画布 ID：`canvas-handoff-c2d99b4221d94d6449dede5b6cb30a32`；重复进入同集复用该 ID。
- 分镜卡片“在画布中打开此分镜”携带 `episodeId` 与 `shotId`；画布 URL 同时携带 `dramaProjectId`、`episodeId`、`shotId`，顶部显示项目/剧集上下文。
- 画布顶部“返回短剧分镜工作台”会返回 `/drama-lab/{projectId}/create?episode={episodeId}&stage=storyboard#storyboard-shot-{shotId}`；返回后 URL 保留剧集、分镜阶段和镜头 hash，目标镜头实际定位到视口内。
- 画布顶部切换剧集使用 `episode-canvas` 服务解析/复用对应集画布，不直接复用其他集 ID；V 原生 Canvas runtime 仍保持独立。
- 测试环境浏览器往返验证通过；未发现重复创建画布或返回丢失镜头定位。任务执行中切换的持久化链路沿用服务端任务 ID，仍列入最终综合验收复测。

### 最终工作台布局与综合验收（2026-09-09）

- 测试环境桌面 1920×1080：分镜工作台完整显示顶部步骤、左侧剧集/镜头列表、中间分镜卡片、右侧团队协作与审批；同一镜头可看到资产引用、提示词、首/关键/尾帧、分镜图、视频和音频区域；页面无横向溢出。
- 测试环境窄屏 390×844：移动布局自动隐藏桌面侧栏，分镜工作台和 7 个分镜卡片正常展示，页面无横向溢出。
- 桌面交互：左侧剧集侧栏和右侧团队协作面板均可收起/展开，收起后主工作区仍保留分镜卡片且无横向溢出。
- 画布闭环：分镜入口携带 `episodeId/shotId`，同一集复用稳定画布 ID；画布返回保留 `stage=storyboard` 与镜头 hash 并定位原镜头。
- 闪烁回归：点击分镜工作台后 5 秒内 `recover-generation` 只请求 1 次；无重复刷新。
- 批量回归：缺少资产时统一提示并跳过，不再逐镜头重复弹窗；取消按钮和服务端任务保留语义已部署。
- 相关自动化回归、TypeScript、Prettier、开发地图/接口索引门禁均通过；当前未发现需要新增 UI 修改的明确缺陷。
- 最终仍保留一项非阻断验收：带完整资产的混合批量样本中途取消及部分成功结果的真实上游验证；不影响已完成的单镜头、画布和缺失资产边界。

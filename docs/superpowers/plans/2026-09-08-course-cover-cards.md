# 课程封面卡片实施计划

用户已确认：只增加后台课程封面上传/替换，学校「课程安排」显示纯封面卡片；桌面三列、平板两列、手机一列。点击保留课程内容与安排；缺图为中性占位。其他板块不改。

- [x] 测试先行：封面真实图片校验、永久归属、保存回读、学校访问权限、引用保护。
- [x] 封面键保存在既有 platform_courses.content.coverStorageKey JSON 字段，无新表/列；新增课程专属上传和受权限保护的封面读取接口，复用媒体存储。
- [x] 后台表单上传、预览、替换和取消清理；学校课程纯图片三列卡片。
- [x] 聚焦测试、类型检查、Lint、全量单测、构建及桌面/390/430 浏览器回归。
- [x] 更新开发文档与待验收说明，保留实际证据，检查差异和 UTF-8。

验收范围：仅本地选图，不提供外链字段；已有课程标题和正文数据保留，纯封面卡片不展示这些文字。todo 已核对，无对应待办需要移除。测试使用隔离本地文件 Provider，不访问服务器凭据或真实渠道。
